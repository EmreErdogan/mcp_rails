{
  "mcpServers": {
    "<%= Rails.application.class.module_parent_name.underscore %>": {
      "command": "ruby",
      "args": ["bin/mcp_server"],
      "cwd": "<%= Rails.root %>",
      "env": {
        "RAILS_ENV": "development"
      }
    }
  }
}